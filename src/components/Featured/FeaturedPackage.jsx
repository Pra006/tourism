import React from 'react'
import './FeaturedPackage.css' // Assuming you have a CSS file for styling the component
import CardGrid from '../CardGrid/CardGrid' // Importing the CardGrid component

const FeaturedPackage = () => {
  return (
    <section>
    <div>FeaturedPackage</div>
    < CardGrid />
    </section>
  )
}

export default FeaturedPackage